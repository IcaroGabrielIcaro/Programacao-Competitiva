#include <bits/stdc++.h>

using namespace std;

int main() {

    int N, K;
    cin >> N >> K;

    vector<int> flores(N);
    for (int i = 0; i < N; i++) {
        cin >> flores[i];
    }

    return 0;

}