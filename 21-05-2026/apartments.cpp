#include <bits/stdc++.h>

using namespace std;

int main() {

    long long n, m, k;
    cin >> n >> m >> k;

    vector<long long> as;
    for (long long i = 0; i < n; i++) {
        cin >> as[i];
    }

    vector<long long> bs;
    for (long long i = 0; i < m; i++) {
        cin >> bs[i];
    }
    
    return 0;

}